{
  "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/servers/main/schema/config.json",
  "_comment": "Minimal MCP configuration for any developer. Start here!",
  "mcpServers": {
    "filesystem": {
      "_description": "Secure file system access with configurable permissions",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${HOME}/projects"
      ]
    },
    "fetch": {
      "_description": "HTTP client for fetching web content as markdown",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch"]
    },
    "memory": {
      "_description": "Persistent knowledge graph for cross-session context",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    }
  }
}
